export(ci, settings.meta, forest, forest.meta,
       funnel, funnel.default, funnel.meta,
       baujat, baujat.meta,
       bubble, bubble.metareg,
       as.data.frame.meta,
       metabias, metabias.default, metabias.meta,
       metabin, metacont, metacor, metacr, metaprop, metarate,
       metareg, metainc, metamean,
       metacum, metagen, metainf, radial, radial.default, radial.meta,
       print.meta, print.metabias,
       metabind, forest.metabind,
       read.rm5, print.rm5, print.summary.rm5, summary.rm5, metabias.rm5,
       read.mtv,
       print.summary.meta, summary.meta,
       trimfill, trimfill.default, trimfill.meta,
       labbe, labbe.default, labbe.metabin,
       update.meta, cilayout, weights.meta,
       gs)

# not visible:
# asin2p, formatPT, meta-internal, logit2p, formatCI, rmSpace, xlab, z2cor,
# is.installed.package, crtitle, catmeth, cathet,
# kentau, hypergeometric, linregcore, setOption, warnarg,
# isquared, calcH, paulemandel, estimate.missing,
# ciWilsonScore, ciAgrestiCoull, ciSimpleAsymptotic,
# is.relative.effect, backtransf,
# hetcalc, chkarg, chklevel, chklogical, formatN, bylabel, asin2ir, Rb,
# setunit, add.label, add.text, add.xlab, draw.axis, draw.ci.square,
# draw.ci.diamond, draw.ci.predict, draw.forest, draw.lines,
# formatcol, removeNULL, tg, tgl, twolines, wcalc, pvalQ,
# is.prop, is.rate, is.log.effect, is.mean

S3method(as.data.frame, meta)
S3method(baujat, meta)
S3method(bubble, metareg)
S3method(forest, meta)
S3method(forest, metabind)
S3method(funnel, default)
S3method(funnel, meta)
S3method(labbe, default)
S3method(labbe, metabin)
S3method(metabias, default)
S3method(metabias, meta)
S3method(metabias, rm5)
S3method(print, meta)
S3method(print, metabias)
S3method(print, rm5)
S3method(print, summary.meta)
S3method(print, summary.rm5)
S3method(radial, default)
S3method(radial, meta)
S3method(summary, meta)
S3method(summary, rm5)
S3method(trimfill, default)
S3method(trimfill, meta)
S3method(update, meta)
S3method(weights, meta)

importFrom("grid", "arrow", "gpar", "grid.draw", "grid.layout",
           "grid.lines", "grid.newpage", "grid.polygon", "grid.rect",
           "grid.text", "grid.xaxis", "textGrob",
           "popViewport", "pushViewport", "viewport", "unit", "unit.c", "convertX")

importFrom("grDevices", "gray")

importFrom("graphics", "abline", "axis", "lines", "par", "plot", "points", "polygon", "text")

importFrom("stats", "as.formula", "binom.test", "coef", "cor", "lm", "pchisq",
           "pnorm", "pt", "qnorm", "qt", "runif", "update", "var", "weighted.mean",
           "weights")

importFrom("utils", "count.fields", "read.table",
           "assignInNamespace", "getFromNamespace",
           "packageDescription", "packageVersion")
